{
  "scripts": {
    "dev": "wrangler dev src/index.ts -e dev",
    "deploy": "wrangler deploy --minify src/index.ts",
    "staging": "wrangler deploy --minify src/index.ts -e staging",
    "production": "wrangler deploy --minify src/index.ts -e production",
    "test": "vitest --run"
  },
  "dependencies": {
    "@twind/core": "^1.1.3",
    "@twind/preset-tailwind": "^1.1.4",
    "hono": "^4.0.5",
    "nanoid": "^5.0.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "@peculiar/webcrypto": "^1.4.5",
    "vitest": "^1.3.1",
    "wrangler": "^3.25.0"
  }
}
